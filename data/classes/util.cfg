{
	get_class: "def(string name) -> class rpg_class
	
	find_or_die(all_classes, value.name = name)
	",

	all_classes: "[class rpg_class] ::
	map([map]<- get_document('data/rpg_classes.cfg'), construct('rpg_class', value))
	",
}
